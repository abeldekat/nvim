-- using sed for color and prefer_light:
local color = 'astrotheme'
local prefer_light = false
-- end using sed

return {
  color = color,
  prefer_light = prefer_light, -- prefer light during this vim session
}
